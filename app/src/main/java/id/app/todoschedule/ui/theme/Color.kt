package id.app.todoschedule.ui.theme

import androidx.compose.ui.graphics.Color

// Dark Theme Colors (Primary)
val DarkBackground = Color(0xFF1A1F3A)  // Dark navy blue background
val DarkSurface = Color(0xFF252B47)     // Slightly lighter surface
val DarkSurfaceVariant = Color(0xFF2D3451) // Card background

// Primary/Accent Colors
val PrimaryBlue = Color(0xFF5B7BFF)      // Bright blue for buttons & active states
val PrimaryBlueDark = Color(0xFF4A67E5) // Darker blue for pressed state

// Priority Colors
val PriorityHigh = Color(0xFFFF5B7B)     // Red/Pink for high priority
val PriorityMedium = Color(0xFFFFB347)   // Orange/Yellow for medium priority
val PriorityLow = Color(0xFF4CAF50)      // Green for low priority

// Text Colors
val TextPrimary = Color(0xFFFFFFFF)      // White for primary text
val TextSecondary = Color(0xFFB0B8CC)    // Light gray for secondary text
val TextDisabled = Color(0xFF6B7280)     // Darker gray for disabled text

// Other Colors
val DividerColor = Color(0xFF353C5A)     // Subtle divider
val ErrorColor = Color(0xFFEF4444)       // Error red
val SuccessColor = Color(0xFF10B981)     // Success green

// Light Theme Colors (for future use)
val LightBackground = Color(0xFFF5F7FA)
val LightSurface = Color(0xFFFFFFFF)
val LightTextPrimary = Color(0xFF1F2937)
val LightTextSecondary = Color(0xFF6B7280)